/*
Package queues provides queue data structures of varying element types.

The underlying queue type is implemented with interface{} elements.
Queues of other types are implemented as typed wrappers around the
interface{} queue.
*/
package queues
